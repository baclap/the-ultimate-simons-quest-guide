# Objset 4 Recipe Promotion Demo

This demo presents the milestone where new save-state probes promoted objset
`4` from diagnostic to validated/projected.

Key evidence:

- Vrad Graveyard day/night: CHR `06/07`
- Castlevania Bridge day/night: CHR `06/07`
- Deborah Cliff night: CHR `06/07`

The previous diagnostic fallback used CHR `08/09`, which caused visibly
scrambled objset `4` renders. The copied before images are from the prior
recipe resolver demo; current images are generated by `npm run render:recipe-atlas`.
